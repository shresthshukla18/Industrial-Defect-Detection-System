# Industrial-Defect-Detection-System
The objective aims to test unsupervised anomaly detection in industrial manufacturing. It compares Autoencoder reconstruction with embedding-based models (PaDiM, PatchCore) using MVTec AD. Built as an offline batch pipeline, it identifies and localizes defects when faulty training data is scarce or unavailable.
